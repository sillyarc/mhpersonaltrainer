rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Adjust to your auth model. This is permissive for prototyping.
    match /{document=**} {
      allow read, write: if request.auth != null;
    }
  }
}
